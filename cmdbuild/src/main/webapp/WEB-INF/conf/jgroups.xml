<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="urn:org:jgroups" xsi:schemaLocation="urn:org:jgroups http://www.jgroups.org/schema/jgroups.xsd">

	<!-- REMEMBER TO SET Djava.net.preferIPv4Stack in environment settings or 
		express addresses in ipv6 otherwise each instance will form a cluster of 
		its own -->
	<TCP bind_port="7800" bind_addr="NON_LOOPBACK"/>
	<PDC cache_dir="jgroups.pdc.cache"/>
	<!-- TCPPING: you have to edit initial hosts according to your cluster. You do not need to decalre all cluster members -->
	<TCPPING async_discovery="true"
		initial_hosts="${jgroups.tcpping.initial_hosts:10.0.0.1[7800],10.0.0.2[7800]}"
		port_range="2" 
		use_disk_cache="true"
		return_entire_cache="true"	
	/>
	<MERGE3 min_interval="10000" max_interval="30000" />
	<FD_SOCK />
	<FD timeout="3000" max_tries="3" />
	<VERIFY_SUSPECT timeout="1500" />
	<pbcast.NAKACK2 use_mcast_xmit="false"
		discard_delivered_msgs="true" />
	<UNICAST3 />
    <RSVP />
	<pbcast.STABLE stability_delay="1000" desired_avg_gossip="50000"
		max_bytes="400000" />
	<pbcast.GMS print_local_addr="true" join_timeout="2000" />
</config>